{
  "name": "habit-heatmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs --prod"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.7.3"
  }
}
